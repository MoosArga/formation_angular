import { SuiviUserDirective } from './suivi-user.directive';

describe('SuiviUserDirective', () => {
  it('should create an instance', () => {
    const directive = new SuiviUserDirective();
    expect(directive).toBeTruthy();
  });
});
