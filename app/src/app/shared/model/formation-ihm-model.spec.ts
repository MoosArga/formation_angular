import { FormationIhmModel } from './formation-ihm-model';

describe('FormationIhmModel', () => {
  it('should create an instance', () => {
    expect(new FormationIhmModel()).toBeTruthy();
  });
});
